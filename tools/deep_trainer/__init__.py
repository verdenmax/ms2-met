"""Deep-learning trainers for metabolic-label DIA validation.

The first implementation is a tabular MLP baseline.  Raw-XIC encoders will
use the same fixed-test and evaluation interface rather than inventing a
second experimental protocol.
"""
