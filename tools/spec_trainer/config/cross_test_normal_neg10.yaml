data:
  train_files:
    - runs/baseline_2da_neg10/features.csv
    - runs/baseline_5da_neg10/features.csv
  test_files:
    - runs/baseline_normal_neg10/features.csv
  test_size: 0.0
  feature_cols: []
  target_col: label

model:
  type: lightgbm
  params:
    boosting_type: gbdt
    objective: binary
    metric: [auc, binary_logloss]
    num_leaves: 15
    learning_rate: 0.02
    feature_fraction: 0.9
    bagging_fraction: 0.8
    min_data_in_leaf: 50
    verbose: -1

training:
  num_boost_round: 1000
  early_stopping_rounds: 200
  valid_size: 0.2

output:
  model_path: runs/spec_trainer/models/cross_test_normal_neg10.txt
  result_path: runs/spec_trainer/results/cross_test_normal_neg10.json
  figures_dir: runs/spec_trainer/figures
